---
globs: tests/**/*.py
alwaysApply: false
---


[TEST][!TIGHT] Write unit tests that are tight, isolated, and follow best practices in Python development.

[NAMING][!NO-PHASES] Never name tests after phases or milestones. Test names should be informative.

[NO-MOCK][!REAL] Never mock or stub. Prefer testing real implementations with real or seeded data. If an external system is unavailable, skip or fail explicitly—do not replace with fakes.

[SCOPE][!FOCUSED] Keep tests tight and focused. Each test should validate a single behavior or responsibility. Do not combine multiple concerns in a single test.

[ISOLATION][!FAST] Ensure test isolation but maintain performance. Test suites must not depend on shared state. Each test suite should set up and tear down its own context cleanly and independently and quickly. A test should never take more than 2 seconds to execute.

[PYTHON][!IDIOMATIC] Use standard Python testing practices. Follow idiomatic patterns using pytest or unittest. Prefer pytest for conciseness and fixture support where applicable.

[QUALITY][!READABLE] Write clear, readable code. Maintain high code quality within tests, just as in production code. Use descriptive names, consistent formatting, and minimal duplication.

[ASSERT][!MEANINGFUL] Aim for meaningful assertions. Tests should fail only when the behavior under test is incorrect, and the failure message should clearly indicate what went wrong.

[PERF][!NO-SLEEPS] Avoid read sleeps where possible.

[INTEGRATION][!WHEN-READY] At the end of a feature set create integration tests where they make sense. Make sure that they are well designed and scalable and that they pass.

reference bounty_hunting/docs/testing_strategy.md

for tests that require a fake website reference
docs/guides/testing_quick_reference.md

[NO-MOCK][!REAL] do not mock
[NO-MOCK][!REAL] never mock anything inside the app or outside the app
[NO-MOCK][!REAL] do not mock if we can avoid it
[NO-MOCK][!REAL] avoid mocking
[DATA][!REAL-DATA] use real data
[NO-MOCK][!REAL] try not to mock
[NO-MOCK][!REAL] do not stub
[NO-MOCK][!REAL] no stubs
[NO-MOCK][!REAL] no mocks
[NO-MOCK][!REAL] no mocking
[NO-MOCK][!REAL] no stubs
[DATA][!REAL-DATA] use real data
[NO-MOCK][!REAL] do not mock
[NO-MOCK][!REAL] avoid making stubs
[DATA][!REAL-DATA] use real data and factories
[DATA][!REAL-DATA] create tests with actual data
[NO-MOCK][!REAL] do not stub anything
[NO-MOCK][!REAL] avoid mocking